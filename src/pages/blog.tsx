import type { NextPage } from "next";
import Layout from "@components/layout/Layout";

const Blog: NextPage = () => {
    return (
        <Layout>
            <h1 className="bg-yellow-200 p-32 text-5xl">Blog</h1>
        </Layout>
    );
};

export default Blog;
