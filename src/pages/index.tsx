import type { NextPage } from "next";
import { Fragment } from "react";

const Home: NextPage = () => {
    return (
        <Fragment>
            {/* Who I am */}
            <div className="min-h-screen bg-gradient-to-b from-teal-300 to-slate-100 p-4 dark:from-gray-800 dark:to-cyan-800"></div>
        </Fragment>
    );
};

export default Home;
